services:
  web:
    build: .
    container_name: school-frontend-php
    ports:
      - "8081:80"
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
      - SESSION_DRIVER=file
      - API_BASE_URL=http://host.docker.internal:3002
    restart: unless-stopped
